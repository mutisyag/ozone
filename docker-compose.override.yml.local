version: '2.2'

services:
  nginx:
    command: /bin/true
    ports:
      - 8080:80

  app:
    build:
      context: .
      args:
        REQUIREMENTS_FILE: requirements/local.txt
    scale: 1
    ports:
      - 8000:8000